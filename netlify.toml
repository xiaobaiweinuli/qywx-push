[build]
  # 发布目录设置为 public
  publish = "public"
  
  # 如果你有构建命令，可以添加
  # command = "npm run build"

[[redirects]]
  # 将根路径重定向到 index.html
  from = "/"
  to = "/index.html"
  status = 200

[[redirects]]
  # 处理 SPA 路由（如果需要）
  from = "/*"
  to = "/index.html"
  status = 200